# move up one directory
alias ..='cd ..'
# log in to vault with ldap
alias vaultldap='vault login -method=ldap username={username}'
# open known hosts
alias known-hosts='vim .ssh/known_hosts

export EDITOR='code -w'
export VISUAL='code -w''
